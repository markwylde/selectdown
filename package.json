{
  "name": "selectdown",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild --servedir=demo/dist --bundle demo/src/*.js --outdir=demo/dist --minify --sourcemap",
    "build": "esbuild --bundle demo/src/*.js --outdir=demo/dist --minify --sourcemap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@emotion/css": "^11.1.3",
    "mithril": "^2.0.4",
    "number-to-words": "^1.2.4"
  },
  "devDependencies": {
    "esbuild": "^0.12.16",
  }
}
